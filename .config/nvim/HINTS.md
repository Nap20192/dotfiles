# Neovim Config — клавиши

> **Лидер:** `Space` | **Локальный лидер:** `,`
>
> Здесь только те бинды, которые реально существуют в конфиге. Устройство конфига —
> в [README.md](./README.md).

---

## Содержание

1. [Базовое](#базовое)
2. [Файлы и деревья](#файлы-и-деревья)
3. [Буферы и окна](#буферы-и-окна)
4. [Поиск](#поиск)
5. [LSP](#lsp)
6. [Автодополнение](#автодополнение)
7. [Отладка (DAP)](#отладка-dap)
8. [AI: Claude Code](#ai-claude-code)
9. [Git](#git)
10. [Go](#go)
11. [Текст и окружения](#текст-и-окружения)
12. [Темы](#темы)
13. [Прочее](#прочее)
14. [Чего здесь нет](#чего-здесь-нет)

---

## Базовое

| Клавиша | Режим | Действие |
|---|---|---|
| `jk` | insert | Выйти в normal |
| `;` | normal | `:` — командный режим |
| `<leader>?` | n | Открыть этот файл |
| `<leader>d` | n | Включить/выключить диагностику |
| `<localleader>sc` | n | Включить/выключить проверку орфографии (`ru_ru,en_us`) |
| `gx` | n | Открыть URL под курсором |
| `gx` | v | Открыть URL в выделении |
| `gV` | n | Выделить последний вставленный/изменённый текст |

Скролл (`<C-u>`, `<C-d>`, `<C-f>`, `zt`, `zz`, `zb`) — обычный, без анимации.
`scrolloff=999`, поэтому курсор всегда держится по центру экрана.

### Копирование путей

| Клавиша | Режим | Действие |
|---|---|---|
| `<leader>cp` | n | Относительный путь файла → буфер обмена |
| `<leader>cP` | n | Абсолютный путь файла |
| `<leader>cp` | v | Относительный путь + номера строк (`./path:10-20`) |
| `<leader>cP` | v | Абсолютный путь + номера строк |

---

## Файлы и деревья

### neo-tree — основное дерево

| Клавиша | Действие |
|---|---|
| `<C-b>` или `<leader>nt` | Открыть/закрыть дерево (справа, ширина 34) |
| `<leader>nr` | Показать текущий файл в дереве |
| `<leader>nb` | Список буферов деревом |
| `<leader>ng` | Git-статус деревом |

**Внутри окна neo-tree:**

| Клавиша | Действие |
|---|---|
| `<CR>` / `l` / `o` | Открыть |
| `h` | Свернуть узел |
| `s` / `S` | Открыть в vertical / horizontal split |
| `t` | Открыть во вкладке |
| `P` | Превью в плавающем окне |
| `a` / `A` | Создать файл / каталог |
| `d` / `r` | Удалить / переименовать |
| `y` / `x` / `p` | Копировать / вырезать / вставить |
| `H` | Показать/скрыть скрытые файлы |
| `z` | Свернуть все узлы |
| `R` / `q` / `?` | Обновить / закрыть / помощь |
| `<leader>ks` | Добавить выбранные записи в контекст Claude |

### mini.files

| Клавиша | Действие |
|---|---|
| `<leader>m` | Открыть на текущем файле |
| `<leader>M` | Открыть на cwd |

**Внутри:** `l` войти, `<CR>` войти+открыть, `h` / `H` наверх, `q` закрыть,
`=` применить изменения, `<BS>` сброс, `@` показать cwd, `m` / `'` метка / переход,
`<` / `>` подрезать колонки, `g?` помощь.

### netrw

`<leader>e` — `:Explore`. Древовидный режим, баннер выключен.

---

## Буферы и окна

| Клавиша | Действие |
|---|---|
| `<M-h>` / `<M-l>` | Предыдущий / следующий буфер |
| `<M-k>` / `<M-j>` | Предыдущий / следующий буфер (дубль) |
| `<C-S-w>` | Закрыть все буферы кроме текущего |
| `<C-h/j/k/l>` | Переход между окнами nvim и панелями tmux (vim-tmux-navigator) |

Открытые буферы показывает mini.tabline сверху, модифицированные помечены `●`.

### Harpoon

| Клавиша | Действие |
|---|---|
| `<leader>ha` | Добавить файл в список |
| `<leader>hh` | Быстрое меню списка |

---

## Поиск

### fff.nvim — файлы и grep

| Клавиша | Действие |
|---|---|
| `<leader>ff` | Найти файл (frecency + fuzzy) |
| `<leader>fg` | Live grep по проекту |

**Внутри пикера:**

| Клавиша | Действие |
|---|---|
| `<CR>` | Открыть |
| `<C-s>` / `<C-v>` / `<C-t>` | Split / vsplit / вкладка |
| `<C-n>` / `<C-p>` или стрелки | Вниз / вверх |
| `<C-u>` / `<C-d>` | Скролл превью |
| `<Tab>` | Отметить файл (мультивыбор) |
| `<C-q>` | Отмеченное → quickfix |
| `<S-Tab>` | Переключить режим grep: plain → regex → fuzzy |
| `<C-Up>` | Предыдущий запрос из истории |
| `<Esc>` | Закрыть |

### fzf-lua

| Клавиша | Действие |
|---|---|
| `<leader>fb` | Пикер буферов |
| `<leader>ft` | Пикер вкладок |
| `alt-j` / `alt-k` | Вниз / вверх внутри fzf |

Остальные пикеры доступны командами: `:FzfLua`, `:FzfLua live_grep`, `:FzfLua lsp_references` и т.д.

---

## LSP

### Навигация (встроенные бинды Neovim)

| Клавиша | Действие |
|---|---|
| `gd` | К определению (`vim.lsp.buf.definition`) |
| `grr` | Все ссылки |
| `gri` | Реализации |
| `grt` | К определению типа |
| `grn` | Переименовать |
| `gra` | Code actions |
| `gO` | Символы документа |
| `<C-s>` (insert) | Signature help |
| `K` | Hover (buffer-local, появляется при подключении LSP) |

### Диагностика

| Клавиша | Действие |
|---|---|
| `]d` / `[d` | Следующая / предыдущая (с плавающим окном) |
| `gl` | Показать диагностику под курсором |
| `<leader>q` | Диагностика в location list |
| `<leader>d` | Включить/выключить диагностику |

### Форматирование

| Клавиша | Действие |
|---|---|
| `<leader>fo` или `:Format` | Форматировать буфер |
| `gq{motion}` | Форматировать через conform (`formatexpr`) |

Форматирование по сохранению включено для всех filetype из `formatters_by_ft`,
для остальных — fallback на LSP.

---

## Автодополнение

blink.cmp, preset `default`. Документация всплывает сама через 500 мс.

| Клавиша | Действие |
|---|---|
| `<CR>` | Принять выбранное (fallback — обычный перевод строки) |
| `<C-y>` | Выбрать и принять |
| `<C-n>` / `<C-p>` | Следующий / предыдущий вариант |
| `<C-e>` | Закрыть меню |
| `<C-space>` | Показать меню / показать документацию |
| `<C-b>` / `<C-f>` | Скролл документации |
| `<Tab>` / `<S-Tab>` | Прыжки по placeholder'ам сниппета |

Скобки закрываются автоматически (blink.pairs), парные подсвечиваются цветом
вложенности.

---

## Отладка (DAP)

| Клавиша | Действие |
|---|---|
| `<F5>` | Старт / продолжить |
| `<F2>` | Остановить сессию и закрыть UI |
| `<F6>` | Открыть REPL |
| `<F7>` | Выполнить до курсора |
| `<F10>` | Step over |
| `<F11>` | Step into |
| `<F12>` | Step out |
| `<leader>Db` | Breakpoint |
| `<leader>DB` | Условный breakpoint (спросит условие) |
| `<leader>Du` | Компактный UI (layout 2) |
| `<leader>DU` | Полный UI |

UI открывается сам при старте сессии и закрывается при завершении.
Значения переменных показываются прямо в коде (virtual text).

---

## AI: Claude Code

Префикс — `<leader>k*`.

### Claude Code — сессия рядом с редактором

Claude запускается **не внутри Neovim**: в панели tmux справа (если nvim в tmux)
или в отдельном окне kitty.

| Клавиша | Режим | Действие |
|---|---|---|
| `<leader>kk` | n | Старт / стоп сессии |
| `<leader>kf` | n | Фокус (для внешнего терминала = то же, что `kk`) |
| `<leader>kr` | n | Продолжить сессию (`--resume`) |
| `<leader>kc` | n | Продолжить последнюю (`--continue`) |
| `<leader>km` | n | Выбрать модель |
| `<leader>kb` | n | Добавить текущий буфер в контекст |
| `<leader>ks` | v | Отправить выделение в контекст |
| `<leader>ks` | n, в neo-tree | Добавить выбранные файлы из дерева |
| `<leader>ky` | n | Принять дифф |
| `<leader>kn` | n | Отклонить дифф |
| `<leader>kS` | n | Статус подключения |

Правки от Claude приходят вертикальным диффом. Внутри диффа можно править текст
до принятия; `:w` тоже принимает, `:q` — отклоняет.

Команды без биндов: `:ClaudeCodeAdd <файл> [start] [end]`, `:ClaudeCodeOpen`,
`:ClaudeCodeClose`, `:ClaudeCodeStart`, `:ClaudeCodeStop`, `:ClaudeCodeCloseAllDiffs`.

---

## Git

Fugitive и друзья, плюс свои бинды в `after/plugin/vim-fugitive.lua`.

| Клавиша | Действие |
|---|---|
| `<leader>gg` | Открыть fugitive UI (`:Git`) |
| `<leader>gb` | Blame текущего файла |
| `<leader>gl` | История репозитория / выделения (`:Gclog`) |
| `<leader>gL` | История текущего буфера |
| `<leader>gy` / `<leader>gY` | Ссылка на строку / файл (`GBrowse`) |

**В окне `:Git`:** `s` stage, `u` unstage, `-` toggle, `=` развернуть, `X` откатить.
**В fugitive-буфере:** `cc` — коммит verbose, `dt` — дифф ревизии под курсором
в новой вкладке. В `git`/`fugitive`-буферах: `<localleader>rs` — soft reset
до ревизии под курсором.

| Команда | Действие |
|---|---|
| `:Git commit`, `:Git push` | Как в CLI |
| `:Gvdiffsplit` / `:Ghdiffsplit` | Дифф с индексом vertical / horizontal |
| `:Gwrite` / `:Gread` | `git add` / `git checkout` для файла |

Запуск внешних команд — `:Dispatch <cmd>`, результат уходит в quickfix.

**diffview.nvim** (дополняет fugitive, не конкурирует):

| Клавиша | Действие |
|---|---|
| `<leader>gv` | Diff против HEAD (файловое дерево + дифф) |
| `<leader>gV` | Закрыть diffview |
| `<leader>gh` | История текущего файла |
| `<leader>gH` | История всего репозитория |

`<leader>gd` занят vim-go (`GoDef`) — поэтому не `gd`/`gD`.

**lazygit** (`<leader>gG`) — плавающий терминал, встроенный `:terminal`,
без отдельного плагина. Выход через `q` внутри lazygit закрывает окно сам.

Терминальный diff — `delta` (`core.pager` в `~/.gitconfig`), красит
`git diff`/`git log -p` в амбер+моно палитру, ту же, что везде.

---

## Go

| Клавиша | Действие |
|---|---|
| `<leader>r` | `:GoRun` |
| `<leader>b` | `:GoBuild` |
| `<leader>t` | `:GoTest` |
| `<leader>f` | `:GoFmt` |
| `<leader>gd` | `:GoDef` |
| `<leader>i` | `:GoImport` |

Форматирование при сохранении делает conform (`goimports` → `gofumpt`), не vim-go.
Inlay hints от gopls включены полностью — типы, имена параметров, поля структур.

Дебаг Go — стандартный DAP-набор выше, конфигурации подхватываются из `dap-go`.

`after/ftplugin/go.lua` подключает свой dispatch-тулинг (`functions/{test,lint}.lua`):

| Клавиша | Действие |
|---|---|
| `<localleader>ta` / `tp` / `tt` | `go test`: всё / текущий пакет / функция под курсором |
| `<localleader>tl` | Повторить последний тест |
| `<localleader>la` / `ll` | golangci-lint: всё / повторить последний |
| `<localleader>ot` | Toggle между `foo.go` и `foo_test.go` |
| `<localleader>ct` / `cT` | Сгенерировать тест для функции / для всех (через `gotests`) |
| `<localleader>oi` | Organize imports (LSP code action) |
| `<localleader>jl` / `sl` | Join / split lines |
| `<localleader>em` / `ef` / `ec` / `ev` | Extract to method / function / constant / variable |
| `<localleader>fs` | Fill struct |
| `<localleader>at` | Add test (gopls' own code action) |

---

## Текст и окружения

### vim-abolish

`:S/шаблон/замена/g` — замена с сохранением регистра и словоформ,
`crs`/`crc`/`crm`/`cru` — snake / camel / mixed / UPPER для слова под курсором.

---

## Темы

| Клавиша | Действие |
|---|---|
| `<leader>th` | Пикер тем (live preview) |

По умолчанию — своя `vnkjd-monochrome`. Под kitty/ghostty фон статусбара прозрачный.
Светлый/тёмный режим статусбара читается из `$XDG_CACHE_HOME/vnkjd/theme`.

---

## Прочее

| Клавиша / команда | Действие |
|---|---|
| `<localleader>hc` | Toggle: визуально скрыть/показать комментарии (treesitter conceal) |
| `<localleader>fm` или `:RenderMarkdown` | Превью markdown через `glow` в сплите |
| `:OpenLink` | Открыть URL под курсором |
| `:Undotree` | Дерево истории изменений (встроенное в Neovim 0.13) |
| `:DiffTool <файл>` | Встроенный дифф-тул Neovim 0.13 |
| `<leader>lc` или `:Leet` | LeetCode (язык по умолчанию — rust) |
| `:DBUI` | Интерфейс баз данных (dadbod) |
| `:Mason` | Менеджер LSP-серверов, форматтеров, линтеров |
| `]<C-q>` / `[<C-q>` | Вперёд / назад по стеку quickfix |
| `]<C-l>` / `[<C-l>` | Вперёд / назад по стеку location list |
| `:checkhealth vnkjd` | Свои проверки |

**dadbod:** подключения сохраняются в `~/.local/share/nvim/dadbod/`, в SQL-буферах
работает автодополнение таблиц и колонок. Для PostgreSQL есть встроенный
helper-запрос **Table Size**.

---

## Чего здесь нет

Раньше в этом файле было описано то, чего в конфиге не существует. Чтобы не искать зря:

- **obs.nvim** и бинды `<leader>n*` для заметок — плагин не установлен
  (`<leader>n*` теперь принадлежит neo-tree);
- **oil.nvim** и бинд `-` — плагин ставится, но его конфиг целиком закомментирован;
- **Copilot и mini.surround** — вырезаны: агент теперь только в терминале,
  автокомплит не нужен (LSP hover/signature вместо него); своей замены на
  `ys`/`cs`/`ds` пока нет — попроси написать, если понадобится;
- **treesitter text objects** (`af`/`if`, `ac`/`ic`, `]f`/`[f`, swap параметров) —
  убраны вместе с остальным мёртвым закомментированным кодом в
  `nvim-treesitter.lua`; фолдинг по treesitter тоже не настроен;
- **fzf-lua бинды** `<leader>fs`, `<leader>fd`, `<leader>fq`, `<leader>fh` и прочие —
  не определены, используй команды `:FzfLua *`;
- **`<leader>ut`** для undotree — бинда нет, есть команда `:Undotree`;
- **`:DiffOrig`** — команды нет, есть `:DiffTool`;
- **переключение вкладок `<A-h>` / `<A-l>`** — эти клавиши перехвачены mini.tabline
  под переключение буферов, вкладки переключаются `gt` / `gT`.

Подробности — в разделе «Известные расхождения» в [README.md](./README.md).
